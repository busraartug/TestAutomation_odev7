package example;

import javax.sound.midi.Soundbank;
import java.util.HashMap;

public class Main {
    public static void main(String[] args) {
for (planets.Planets planetx : planets.Planets.values()){
    System.out.println(planetx + " Distance to sun: " + planetx.getUzaklik() + " Arrangment:" + planetx.getSiralama() + " radius(r): " + planetx.getYaricap() + " Ratotion time:" + planetx.getDonmesuresi()
    );

}
    }
}
